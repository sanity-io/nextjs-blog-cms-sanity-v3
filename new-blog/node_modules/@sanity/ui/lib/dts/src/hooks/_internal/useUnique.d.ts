/**
 * This is a React hook to make sure that a record identity is the same on every render. Uses strict
 * equality comparison (eg by identity), and only goes one level deep.
 *
 * @internal
 */
declare type Comparable = Record<string | number | symbol, unknown> | undefined | null;
export declare function useUnique<ValueType extends Comparable = Comparable>(value: ValueType): ValueType;
export {};
//# sourceMappingURL=useUnique.d.ts.map