"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractTextFromBlocks = extractTextFromBlocks;
exports.isPortableTextArray = isPortableTextArray;
var _types = require("@sanity/types");
function isPortableTextArray(blocks) {
  return Array.isArray(blocks) && (blocks.length === 0 || blocks.some(_types.isBlock));
}
function extractTextFromBlocks(blocks) {
  var firstBlock = blocks.find(_types.isBlock);
  if (!firstBlock || !firstBlock.children) {
    return '';
  }
  return firstBlock.children.filter(_types.isSpan).map(span => span.text).join('');
}