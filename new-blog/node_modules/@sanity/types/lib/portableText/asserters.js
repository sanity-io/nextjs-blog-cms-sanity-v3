"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isBlock = isBlock;
exports.isSpan = isSpan;
function isRecord(value) {
  return !!value && (typeof value == 'object' || typeof value == 'function');
}
function isBlock(value) {
  return isRecord(value) && typeof value._type === 'string' &&
  // block types can be named, so expect anything here.
  typeof value.style === 'string' && Array.isArray(value.children) && Array.isArray(value.markDefs);
}
function isSpan(value) {
  return isRecord(value) && value._type === 'span' && typeof value.text === 'string' && Array.isArray(value.marks);
}