import type { CurrentUser } from '@sanity/types';
import { Dispatch, ReactNode } from 'react';
import { RecentSearchesStore } from '../../datastores/recentSearches';
import { SearchAction, SearchReducerState } from './reducer';
interface SearchContextValue {
    dispatch: Dispatch<SearchAction>;
    state: SearchReducerState;
    recentSearchesStore?: RecentSearchesStore;
}
interface SearchProviderProps {
    children?: ReactNode;
    currentUser: CurrentUser;
}
/**
 * @internal
 */
export declare function SearchProvider({ children, currentUser }: SearchProviderProps): JSX.Element;
export declare function useSearchState(): SearchContextValue;
export {};
//# sourceMappingURL=index.d.ts.map