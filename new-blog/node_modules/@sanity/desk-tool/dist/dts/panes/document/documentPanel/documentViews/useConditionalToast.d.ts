import { ToastParams } from '@sanity/ui';
/**
 * Workaround to support conditional toast (e.g. a toast that is visible as long as a condition holds true)
 * @param params
 */
export declare function useConditionalToast(params: ToastParams & {
    id: string;
    enabled?: boolean;
}): void;
//# sourceMappingURL=useConditionalToast.d.ts.map