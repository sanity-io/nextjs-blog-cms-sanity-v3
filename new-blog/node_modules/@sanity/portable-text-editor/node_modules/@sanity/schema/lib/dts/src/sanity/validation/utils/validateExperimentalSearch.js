import { isPlainObject } from 'lodash';
export var validateExperimentalSearch = function (configs) {
    if (!Array.isArray(configs)) {
        return ['The search config of a document type must be an array of search config objects'];
    }
    return configs
        .map(function (conf) {
        if (!isPlainObject(conf)) {
            return 'Search config must be an object of {path: string, weight: number}';
        }
        return null;
    })
        .filter(Boolean);
};
//# sourceMappingURL=validateExperimentalSearch.js.map