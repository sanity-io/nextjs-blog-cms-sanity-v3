import { resolveJSType } from './resolveJSType';
export function resolveTypeName(value) {
    const jsType = resolveJSType(value);
    if (jsType !== 'object') {
        return jsType;
    }
    const obj = value;
    return ('_type' in obj && obj._type) || jsType;
}
//# sourceMappingURL=resolveTypeName.js.map