export var PRESERVE_KEYS = new WeakMap();
export function withPreserveKeys(editor, fn) {
    var prev = isPreservingKeys(editor);
    PRESERVE_KEYS.set(editor, true);
    fn();
    PRESERVE_KEYS.set(editor, !prev);
}
export function isPreservingKeys(editor) {
    return PRESERVE_KEYS.get(editor);
}
//# sourceMappingURL=withPreserveKeys.js.map