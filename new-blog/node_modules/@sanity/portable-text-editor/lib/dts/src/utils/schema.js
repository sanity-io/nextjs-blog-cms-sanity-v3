import Schema from '@sanity/schema';
export function compileType(rawType) {
    return Schema.compile({
        name: 'blockTypeSchema',
        types: [rawType],
    }).get(rawType.name);
}
//# sourceMappingURL=schema.js.map