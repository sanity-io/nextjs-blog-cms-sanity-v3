import { createContext, useContext } from 'react';
/**
 * A React context for sharing the editor value.
 */
export var PortableTextEditorValueContext = createContext(undefined);
/**
 * Get the current editor value from the React context.
 */
export var usePortableTextEditorValue = function () {
    return useContext(PortableTextEditorValueContext);
};
//# sourceMappingURL=usePortableTextEditorValue.js.map