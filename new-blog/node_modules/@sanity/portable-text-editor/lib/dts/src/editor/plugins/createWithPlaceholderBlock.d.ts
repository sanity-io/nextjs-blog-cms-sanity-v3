import { PortableTextSlateEditor } from '../../types/editor';
import { PortableTextFeatures } from '../../types/portableText';
interface Options {
    portableTextFeatures: PortableTextFeatures;
    keyGenerator: () => string;
}
/**
 * Keep a "placeholder" block present when the editor is empty
 *
 */
export declare function createWithPlaceholderBlock({ portableTextFeatures, keyGenerator, }: Options): (editor: PortableTextSlateEditor) => PortableTextSlateEditor;
export {};
//# sourceMappingURL=createWithPlaceholderBlock.d.ts.map